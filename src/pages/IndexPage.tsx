import React from 'react'
import {Box} from "@mui/material"

type IndexPageProps = {}

export const IndexPage: React.FC<IndexPageProps> = ({}) => {

  return (
    <Box>
        Index Page
    </Box>
  )
}